<Window x:Class="mc_statstalker.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mc_statstalker"
        mc:Ignorable="d"
        Title="MC Stat Stalker" Height="450" Width="800">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <ListBox/>
            <Button Content="Add" Click="AddButton_Click"/>
            <Button Content="Delete" Click="RemoveButton_Click"/>
        </StackPanel>
        <DataGrid DockPanel.Dock="Bottom" ItemsSource="{Binding PlayerStats}"/>
    </DockPanel>
</Window>
